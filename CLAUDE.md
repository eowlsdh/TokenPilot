@AGENTS.md

# Claude Code

- Follow the nearest nested `AGENTS.md` under `Sources/TokenApp`,
  `Sources/TokenCore`, or `Tests` for directory-specific work.
- Never inspect or expose provider credential stores while validating local
  usage adapters.
- Distinguish CommandLineTools/Xcode environment blockers from source defects.
- Run `/memory` if this file or its import does not appear to be applied.

